# 端到端流程审计 v0.4 · 2026-07-07

## 审计方法
模拟完整玩家通关路径：邮件→聊天→已删除→浏览器→相册→购物→文件→记事本→结局。逐个模块追踪代码执行。

## 完整通关路径模拟

### P1 · 邮箱 (C01)
✅ 打开邮箱 → 8封普通邮件正确渲染
✅ 点击 M1 → 内容显示正确（含 `node-project.dev/share/linye` 入口）
✅ C01 触发 → `foundClues: [C01]` → 邮箱图标角标点亮
✅ 返回按钮 `mailBack` 正确恢复列表

### P2 · 聊天 (C04, C17, C19-frag1)
✅ 打开聊天 → 默认显示 duya 对话
✅ C04 (duya对话) + C17 (生日0412+认识年份2018) 正确触发
✅ 切换到 N → 看到 `6a2c8f` 密钥前半段
✅ 切换到陈姐 → C19 fragment `chenjieChat` 标记

### P3 · 已删除邮件 (C02, C03)
✅ 拼合：生日0412 + 年份2018后两位 → `041218`
✅ 密码输入 `041218` → 正确解锁 → `deletedMailUnlocked: true`
✅ 已删除3封 + 草稿1封 正确渲染
✅ 点击 mr2 → 查看原始信息 → IP `47.109.82.33`
✅ 点击 m5 → C03 触发（含周明远和G3 meta提示）

### P4 · 浏览器登录 (C05, C07, C08, C09, C16)
✅ 搜索历史 9条 → C05 触发
✅ 书签 → Project Null → 登录页渲染
✅ 密码 `6a2c8f-RGK2024NOV` → 登录成功
✅ C07, C08, C16, C09 全部触发
⚠ **BUG-1: 登录成功页"返回搜索历史"链接**
   `onclick="_H_showSearchHistory(bd.parentElement)"` 
   → `_H_showSearchHistory` 不在全局作用域，在 `window.Haveren` 下
   → 点击产生 ReferenceError，链接无法跳转

### P5 · 相册 (C06, C10, C11, C12, C13)
✅ 47张照片网格渲染
✅ G03 → EXIF 显示 `RGK2024NOV` → C06, C10 触发
✅ G17 → 清迈提示 → C11 触发
✅ G41 → 扫描二维码 → C12 触发
✅ 密码输入 `09140603020711` → C13 触发 → 隐藏页解密
⚠ **ISSUE-1: G41密码提示的7张异常照片排序**
   `anomalyNums = [9, 14, 6, 3, 2, 7, 11]` 与修改时间排序一致
   但 G2/G3/G6/G7/G9/G11/G14 中只有 7 张是异常 → ✓
   `clueNums` 含 17(G17清迈)和 41(G41二维码) → 它们也亮橙色边框
   玩家可能困惑为什么 G17/G41 不算在"7张异常"里

### P6 · 购物 (C18)
✅ 16笔订单渲染
✅ 4笔连续订单 (6/8-6/11) 末两位高亮
✅ 点击任意 → mirror page → 末两位拼接 `47280901`
✅ C18 触发

### P7 · 文件 (C07, C08, C09, C16)
✅ Project Null 已登录 → 直接显示文件列表
✅ key_fragment_3.txt → `null2024` → C07 触发
✅ data_raw.xlsx → C08, C09 触发
✅ data_modified.xlsx → 前三行隐藏（折叠行）→ 100ms 后显示
✅ checksum.txt → C16 触发
⚠ **ISSUE-2: 浏览器登录后刷新文件窗口**
   调用 `renderFileList` → `bindFilesEvents` 中又调用一次 `renderFileList`
   双层渲染，无功能影响但浪费一次 DOM 重建

### P8 · 记事本 (C14, C15, C19-frag2)
✅ 笔记1 → 密码 `null2024` → 解锁 → 瑞康临床数据
✅ 笔记2 → C14 触发 + C19 fragment 标记
⚠ **BUG-2: corrupted localStorage 会导致笔记页面崩溃**
   `buildNotesApp()` 中 `State.get('unlockedNotes')['note1']` 
   → 如果旧存档 `unlockedNotes: null`（JSON spread 覆盖 DEFAULTS）
   → `null['note1']` → TypeError，整个窗口白屏

### P9 · C19 冒充寄件闭环
✅ 陈姐聊天 → fragment `chenjieChat`
✅ 浏览器搜索"快递拦截" → fragment `searchExpress`
✅ 笔记2 → fragment `note2Warning`
✅ 三段全齐 → C19 触发 → `foundClues` 新增 C19
⚠ **ISSUE-3: C19 不在任何应用映射中**
   `hasClueInApp` 遍历 mail/browser/chat/notes/files/photos/shop
   C19 不在其中 → 没有应用图标会因 C19 点亮
   设计上合理（跨应用复合线索），但可能让玩家困惑

### P10 · 笔记3 (C15)
✅ `enoughAppsForNote3()` → 需 ≥5 应用有线索
   在此路径下：mail(C01-03) + browser(C05) + chat(C04,17) + 
   notes(C14) + files(C08,16) + photos(C06,10,11,12,13) + shop(C18) = 7 应用 ✓
✅ 笔记3卡片出现 → 点击 → C15 触发

### P11 · 结局判定
✅ 所有线索到位：C01-C19，除 C19 外全部关联应用
✅ C08 + C09 + C14 + C15 + C16 + C17 → 结局 D（真结局）
✅ BUG-1 不影响结局：C07 通过文件确认触发，登录页的 onclick bug 不阻塞核心流程

## Bug 清单

| # | 严重 | 描述 | 影响 |
|---|------|------|------|
| BUG-1 | 🟡中 | 浏览器登录成功页"返回搜索历史"链接 `_H_showSearchHistory` 未全局暴露 | 点击报错、链接失效 |
| BUG-2 | 🟡中 | `buildNotesApp()` 缺 `unlockedNotes` null 守卫 → 旧存档崩溃 | 损坏存档导致笔记窗口白屏 |
| ISSUE-1 | 🟢轻 | G17/G41 实心橙色边框 + 异常照片边框一致 → 视觉误导 | 玩家可能以为有9张异常照片 |
| ISSUE-2 | 🟢轻 | 浏览器登录后文件窗口双层 `renderFileList` | 无功能影响，浪费 CPU |
| ISSUE-3 | 🟢轻 | C19 无应用映射 → 无图标因冒充寄件闭环点亮 | 玩家完成 C19 后没视觉反馈 |

## 修复优先级
1. **BUG-1**: 把 `_H_showSearchHistory` 暴露到 `window` 或在 onclick 中用 `window.Haveren._H_showSearchHistory`
2. **BUG-2**: 在 `buildNotesApp()` 开头加 `var ul = State.get('unlockedNotes') || {};` 
3. ISSUE-1~3: 可在下一轮 polish 中一并处理

## 正常功能确认
✅ 窗口拖拽、最小化、关闭、最大化
✅ 任务栏标签页切换
✅ 线索计数器实时更新
✅ 图标角标随线索发现点亮
✅ localStorage 存储/读取
✅ 结局覆盖层动画
✅ 所有密码验证逻辑
✅ 已删除邮件 + 草稿箱内容
✅ 浏览器搜索 + 书签展开
✅ 聊天三联系人切换
✅ 相册47张 + 7张异常标注 + 排序提示
✅ 购物16笔 + 4笔高亮 + mirror page
✅ 文件6个 + Excel 对比表格 + 折叠行
✅ 笔记1加密 → 解锁 → 笔记2 → 笔记3渐进出现
